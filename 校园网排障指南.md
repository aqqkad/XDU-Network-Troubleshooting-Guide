# 校园网排障指南

---

## 怎么使用这份指南

先判断自己使用的是哪种接入方式：

- **学校无线网**：连接 `stu-xdwlan`，备用 `stu-wlan`，然后打开网页认证。
- **宿舍有线网**：网线接桌下 RJ45 网口，电脑或路由器通过 PPPoE 拨号。
- **校外访问校内资源**：使用 aTrust，不属于校园网接入故障。

再按现象查下面 9 类问题：

1. 无线已连接，但没网或打不开认证页
2. 认证页能打开，但认证、注销或换账号失败
3. 账号密码、设备绑定或在线状态异常
4. 运营商套餐没有生效
5. 有线 PPPoE 拨号失败
6. 网络频繁掉线或反复认证
7. 网速慢、波动或疑似被限速
8. 只有部分网站、应用或游戏打不开
9. 校外访问、系统兼容与报修

每类问题都按以下顺序组织：

1. **先做**：命中率最高、风险最低的步骤。
2. **仍不行再做**：官方或现场使用过的补充方法。
3. **何时报修**：避免无限重复尝试。

---

## 一、基础信息速查

| 项目 | 内容 |
| --- | --- |
| 学生无线网 | `stu-xdwlan`（优先）、`stu-wlan`（备用） |
| 不应连接 | `xd-wlan` 为教职工网络；`stw-xdwlan` 等相似名称不是正确学生热点 |
| 无线认证入口 | `https://w.xidian.edu.cn`、`https://10.255.44.33` |
| 有线接入 | 宿舍桌下或桌侧 RJ45 网口，通过 PPPoE 拨号 |
| 不能接线的位置 | 墙上或桌边的白色无线 AP 盒、圆形同轴电视口 |
| 校园网账号 | 学号；职工使用工号 |
| 初始上网密码 | 身份证号后六位；末位为 X 时注意大小写 |
| 免费校园网 | 原始资料记录为约 100Mbps、每月 20GB 免费额度；超出一般不额外收费，但部分娱乐类流量会受限，现行规则以 `zfw` 和学校通知为准 |
| 设备数量 | 原始资料的通用口径为每账号最多 6 台；不同运营商产品可能另有规则，以 `zfw` 实际提示为准 |
| 路由器共享 | 路由器拨号通常按一个在线终端计算，路由器下设备共享该连接的带宽与账号额度 |
| IP 与 DNS | 默认均为自动获取 |
| 自服务平台 | `https://zfw.xidian.edu.cn` |
| 校内测速 | `https://test.xidian.edu.cn` |
| 信息网络报修 | `https://repair.xidian.edu.cn` |
| 校外访问 | aTrust，入口 `https://atrust.xidian.edu.cn` |

### 三套密码不要混用

- **校园网上网密码**：用于无线认证、有线拨号和 `zfw`，初始值通常为身份证后六位。
- **统一身份认证密码**：用于一站式、教务、VPN 和报修系统。
- **邮箱等其他系统密码**：独立管理，不要拿来尝试校园网登录。

---

## 二、第一次接入

### 1. 无线接入

1. 连接 `stu-xdwlan`；找不到或无法使用时再试 `stu-wlan`。
2. 暂时关闭手机流量、代理、VPN、Clash 和浏览器代理。
3. DNS 保持自动获取。
4. 等系统弹出认证页；未弹出时用系统浏览器打开：
   - `https://w.xidian.edu.cn`
   - `https://10.255.44.33`
5. 账号填学号，密码填校园网上网密码，免费校园网用户选择“校园网”。
6. 首次成功后可在 `zfw` 中开启无感知认证。

> 若设备启用了随机 MAC、私有 Wi-Fi 地址或随机硬件地址，建议先关闭，否则可能无法建立稳定的无感认证绑定。

### 2. Windows 有线接入

1. 用网线连接电脑和宿舍桌下 RJ45 网口。
2. 关闭 Wi-Fi，避免误把无线连接当成有线成功。
3. Windows 11：设置 → 网络和 Internet → 拨号 → 设置新连接 → 连接到 Internet → 宽带（PPPoE）。
4. Windows 10：控制面板 → 网络和共享中心 → 设置新的连接或网络 → 连接到 Internet → 宽带（PPPoE）。
5. 填写：
   - 用户名：学号；使用运营商套餐时按要求添加后缀。
   - 密码：校园网上网密码。
   - PPPoE 服务名称：**留空**。
6. 将新建的拨号连接命名为纯英文，不要使用中文、空格或特殊符号。
7. IP 与 DNS 均保持自动获取，然后连接。

插线后出现“未识别的网络”不一定是故障；PPPoE 尚未拨通时出现这一提示属于正常现象。

### 3. macOS 与 Linux 有线接入

- 直接让去B站搜

### 4. 宿舍路由器接入

1. 桌下网口连接路由器 **WAN/Internet** 口，不要接白色 AP 盒。
2. 先用电脑直连拨号一次，确认账号和墙口正常，再配置路由器。
3. 连接路由器默认 Wi-Fi，打开机身标签上的管理地址。
4. 上网方式选择“宽带拨号 / PPPoE”，不要选 DHCP 自动获取。
5. 填写账号密码，PPPoE 服务名称留空。
6. 保存后等待 1～2 分钟，查看 WAN 状态是否已连接。

打不开管理页时，可用 `ipconfig` 查看默认网关；仍打不开可在确认配置可重做后恢复路由器出厂设置。

---

## 三、九类故障排查

### 问题 1：无线已连接，但没网或打不开认证页

典型表现：显示“无 Internet”、认证页不弹、`w.xidian.edu.cn` 或 `10.255.44.33` 一直转圈、拒绝连接或白屏。

#### 先做

1. 确认连接的是 `stu-xdwlan` 或 `stu-wlan`。
2. 随便打开一个网页测试。已经能正常上网时，可能是无感认证自动上线，无需再次登录。
3. 关闭代理、VPN、Clash、浏览器代理和手机热点。
4. 把 IPv4、IPv6 的 DNS 都改回自动获取。
5. 用系统浏览器或无痕窗口打开：
   - `https://w.xidian.edu.cn`
   - `https://10.255.44.33`
6. 关闭随机 MAC／私有 Wi-Fi 地址／随机硬件地址。
7. 断开 Wi-Fi → 关闭 Wi-Fi → 重启设备 → 重新连接并认证。

#### 仍不行再做

保留以下认证入口与参数切换方法；不同区域或门户版本可能需要不同参数：

- `https://10.255.44.33/srun_portal_success?ac_id=1`
- `https://10.255.44.33/srun_portal_success?ac_id=8`
- 将 `ac_id` 在 `1` 与 `8` 之间切换。
- HTTPS 证书被浏览器拦截时，可在确认地址确为学校认证入口后，从“高级”继续访问；也可尝试对应的 HTTP 入口。
- 页面白屏、验证码不显示时，关闭浏览器或系统深色模式，刷新页面并等待资源加载。
- QQ、微信内置浏览器异常时，把完整链接复制到 Edge、Chrome 或 Safari。
- 保持连接学校 Wi-Fi，打开“学在西电”App，尝试使用其中出现的认证弹窗。
- 在 `zfw` 的“在线信息”中下线旧会话，并删除对应无感认证后重连。

#### 何时报修

- 同一位置多名同学都打不开认证页。
- 某个 AP 覆盖范围内长期满格无网。
- 完成上述步骤后仍无法访问任何认证入口。

### 问题 2：认证页能打开，但认证、注销或换账号失败

典型表现：`Authentication fail`、设备不在认证范围、验证码异常、注销按钮无响应、显示已在线却不能上网、换账号后仍自动登录旧账号。

#### 设备不在认证范围

1. 关闭设备的随机 MAC：
   - iPhone/iPad：关闭该热点的“私有 Wi-Fi 地址”。
   - Android：改为“设备 MAC”。
   - Windows：关闭“使用随机硬件地址”。
2. 断开后重新连接并认证。
3. 必要时在 `zfw` 中删除旧绑定，再用固定 MAC 登录一次。
4. 手动添加 MAC 时，按页面要求使用小写字母和冒号格式，例如 `a0:b1:c2:d3:e4:f5`。

#### `Authentication fail`

1. 先用无痕窗口重试，排除旧 cookie 与会话。
2. 将认证产品改为“校园网”测试。
3. 校园网可以登录、运营商产品不能登录时，通常是当前位置未接入该运营商（如图书馆仅支持校园网，北校区部分楼栋接入不全）、套餐未生效或产品配置异常。
4. 换到支持该产品的区域，或提交报修核对楼栋接入配置。

#### 注销、换账号与旧会话

无线认证页面的 **pro 主题存在注销按钮无法正常工作的情况**。需要注销时，按以下步骤操作：

1. 查看浏览器地址栏，将 URL 中的 `theme=pro` 改成 `theme=lite` 后重新打开页面。例如：
   - 修改前：`https://w.xidian.edu.cn/srun_portal_success?ac_id=8&theme=pro`
   - 修改后：`https://w.xidian.edu.cn/srun_portal_success?ac_id=8&theme=lite`
2. 在 lite 主题页面中点击“注销”。
4. 也可以登录 `zfw` →“在线信息”，将对应设备下线。
5. 仅下线后设备马上自动登录时，再到“用户 → 无感知认证”删除该设备的绑定或关闭无感认证，然后断开 Wi-Fi 并重新连接。
6. 有线用户在系统中断开宽带连接；启用了自动重拨时先取消自动重拨。
7. 换账号时，先注销并删除旧账号下该设备的无感认证，再断开 Wi-Fi、重新连接并使用新账号登录。

### 问题 3：账号密码、设备绑定或在线状态异常

#### 账号密码先这样核对

1. 账号为学号，不是姓名、手机号或邮箱。
2. 初始密码为身份证后六位；改过密码后必须使用新密码。
3. 关闭中文输入法，检查大小写、空格和复制时带入的字符。
4. 用 `https://zfw.xidian.edu.cn` 验证：
   - 能登录：密码基本正确，继续排查产品、后缀或连接方式。
   - 不能登录：使用“忘记密码”。
5. 改密后，无线认证、有线拨号、路由器和自动拨号配置中的旧密码都要同步更新。

#### 收不到短信验证码

1. 在 `zfw` 的用户信息中核对绑定手机号。
2. 检查欠费停机、短信拦截和骚扰箱。
3. 换时间重试，学校短信服务故障时个人反复发送也无效。
4. 临时账号未绑定手机号时，改用正式账号或人工处理。
5. 自助仍失败可拨打 `88201252`，或工作日携带证件到图书馆 A 区一站式服务大厅相关工位处理。

#### 反复要求登录、设备互踢

1. 关闭随机 MAC，开启无感认证。
2. 在 `zfw` 清理不再使用的在线设备和 MAC 绑定。
3. 只下线、不删除无感认证时，设备可能马上自动上线。
4. 智能插座、时钟等没有浏览器的设备，可按官方说明通过 MAC 无感认证授权。
5. 有线与无线排障时不要同时开着，先保留一种连接方式确认。

### 问题 4：运营商套餐没有生效

典型表现：办了套餐仍显示免费额度、仍走校园网、加后缀反而报错、运营商选项不存在，或只有某些区域不能使用。

#### 正确配置

1. 登录 `zfw` → 产品 → 产品使用优先级。
2. 将所办运营商产品放到最上面，校园网产品放到下面。
3. 无线认证时选择已办理的运营商，账号仍填学号。
4. 有线或路由器 PPPoE 用户名按运营商添加后缀：
   - 电信：`学号@dx`
   - 联通：`学号@lt`
   - 移动：`学号@yd`
5. 网站和 `zfw` 登录通常不加后缀；后缀主要用于 PPPoE 拨号。
6. 调整后在“在线信息”下线旧会话，再重新认证或拨号。

#### 套餐仍未出现

1. 新办套餐可能需要次日或约 24 小时完成同步。
2. 检查后缀是否与所办运营商匹配，套餐是否过期。
3. 去掉后缀能拨通、加后缀不能拨通，通常说明账号本身正常，问题位于套餐开通、同步或后缀。
4. `zfw` 中看不到运营商产品时，联系办理渠道核对是否已绑定本人学号、是否漏开通或办成了其他产品。
5. 某些楼栋或区域可能没有对应运营商接入；免费校园网能认证而运营商产品报 `Authentication fail` 时，应核对区域接入。

运营商套餐不应被当作修复 Wi-Fi 覆盖、丢包、墙口损坏或设备驱动问题的手段；这些问题仍需排障或报修。

### 问题 5：有线 PPPoE 拨号失败

#### 先做通用检查

1. 确认网线接在桌下 RJ45 网口，而不是白色 AP 盒或圆形同轴口。
2. 重新插紧两端，观察网卡指示灯；有条件时换一根已确认可用的网线。
3. 关闭 Wi-Fi、VPN、代理和其他拨号软件。
4. 确认 IP、DNS 自动获取，PPPoE 服务名称留空。
5. 核对用户名：免费校园网使用学号，运营商产品使用正确后缀。
6. 用 `zfw` 验证账号密码。
7. 删除旧宽带连接，重新建立一个 PPPoE 连接，并把**拨号连接名称改成纯英文**。
8. 在 `ncpa.cpl` 中禁用再启用“以太网”，然后重拨。

#### 拨号连接名称必须使用英文

以下现象应优先检查拨号连接名称：

- 代理、VPN、Clash 或虚拟网卡软件开启后，PPPoE 拨不上或无法识别连接。
- 手动偶尔能连接，但经常失败、随机断开或重拨异常。
- `rasdial`、任务计划和自动重拨脚本提示找不到连接，或执行后没有效果。
- 重启后旧连接异常，需要频繁删除、重建。

处理方法：

1. 按 `Win+R`，输入 `ncpa.cpl`。
2. 将 PPPoE 拨号连接改成**纯英文名称**；不要使用中文、空格和特殊符号。
3. 关闭代理、VPN、Clash 和其他虚拟网卡软件后重新拨号；需要代理时，等 PPPoE 成功连接后再开启。
4. 自动拨号脚本、任务计划和 `rasdial` 命令中的名称必须同步改成完全相同的英文名称。

这里说的是 Windows 中显示的**拨号连接名称**；PPPoE 配置里的“服务名称”仍然必须留空，两者不要混淆。

#### 根据错误码处理

| 错误码 | 优先判断 | 处理 |
| --- | --- | --- |
| 619、621 | 旧连接、端口或链路状态异常 | 删除旧连接重建；禁用再启用网卡；拔插或更换网线；关闭其他拨号软件 |
| 628、629 | 远端关闭连接，常见于账号、会话或套餐 | 验证账号密码；核对后缀与产品优先级；下线旧会话；确认套餐已生效；重建连接 |
| 633 | 连接正被占用或拨号配置异常 | 断开已有连接；检查电脑、路由器或其他设备是否仍在拨号；关闭其他拨号程序；重建连接 |
| 638 | PPPoE 请求超时，外置 USB 网卡需重点排查驱动 | 换 USB 口或内置网卡；重装外置网卡驱动；重建连接；换线、换电脑交叉测试 |
| 651 | 网卡、网线、墙口或线路层异常 | 重插或换网线；禁用再启用网卡；绕过路由器直连；用另一台电脑测试同一端口；确认后报修 |
| 654、678 | 对端无应答或链路异常 | 重建连接；重插网线；禁用再启用网卡；核对账号与服务名称；交叉测试后报修 |
| 691 | 用户名、密码、后缀或认证域错误 | 在 `zfw` 验证密码；核对是否应添加运营商后缀；重置上网密码 |
| 718 | 等待远端响应超时 | 验证账号；重建连接；重置网卡；稍后重拨；持续发生则报修 |
| 720 | WAN Miniport 或网络协议驱动异常 | 在设备管理器显示隐藏设备，卸载 `WAN Miniport (IP)` 后重启；再重建拨号连接 |
| 728、813 | 会话、连接配置或链路异常 | 参照 628/629：核对账号和套餐、重建连接、重置网卡、交叉测试 |

错误码可能随着链路状态变化，不必机械地只执行表格中的一行。优先完成通用检查，再做交叉验证。

#### 哪些情况要更新或重装网卡驱动

出现以下任一现象时，应把网卡驱动列为主要排查项：

- 同一根网线、同一墙口，舍友电脑正常而自己的电脑失败。
- 以太网或 Wi-Fi 网卡偶尔消失、带黄色感叹号，或禁用后无法重新启用。
- 睡眠、合盖、重启或系统更新后开始掉线。
- 拨号没有反应，或反复出现 638、651、720 等错误。
- 使用 USB 转 RJ45、USB 无线网卡时频繁超时、断开。
- 物理链接速率异常降到 100Mbps，而网线和墙口交叉测试正常。

处理方法：

1. 在设备管理器中确认有线／无线网卡的准确型号。
2. 优先从电脑品牌官网或网卡芯片厂商官网下载匹配当前系统的驱动；不要只依赖第三方驱动工具。
3. 先更新驱动并重启；仍异常时卸载对应网卡设备和驱动，重启后重新安装。
4. PPPoE 报 720 或拨号完全没有反应时，除实体网卡驱动外，还要单独处理 `WAN Miniport (IP)`。
5. 如果故障正好在更新驱动后出现，可在设备管理器中回退到此前稳定版本。

#### 拨号完全没有反应

1. 打开设备管理器 → 网络适配器 → 查看隐藏设备。
2. 检查 `WAN Miniport` 是否缺失或带异常标志。
3. 卸载 `WAN Miniport (IP)`，重启让 Windows 自动重装。
4. 网卡带黄色感叹号时，重新安装对应有线网卡驱动。
5. 最后才使用 Windows“网络重置”；它会清除网络配置，之后需要重新连接 Wi-Fi 和建立拨号。

#### 开机或断线后自动拨号

PPPoE 默认不会随开机自动连接，“记住密码”不等于自动拨号。

可选方法：

1. `ncpa.cpl` → 宽带连接属性 → 选项 → 勾选“断开后重新连接”。部分 Win11 版本可能没有该选项。
2. 任务计划程序在“启动时”或“登录时”运行 `rasdial`。
3. 先在拨号连接中保存密码，再把仅含 `rasdial "CampusNet"` 的批处理快捷方式放到启动目录；`CampusNet` 必须与实际英文连接名完全一致。
4. 使用路由器拨号，由路由器自动保持连接。

如果在 `rasdial` 参数或任务计划中直接写入账号密码，密码会以明文保存；共享电脑不建议这样做。

#### 怎样判断是电脑、网线还是墙口

进行两次交叉测试：

1. 用舍友已确认正常的网线连接自己的电脑。
2. 用舍友已确认正常的电脑连接自己的墙口。

判断结果：

- 换电脑后正常：重点检查本机网卡、驱动和拨号配置。
- 换线后正常：原网线损坏或接触不良。
- 多台电脑、多根网线在同一墙口都失败：墙口或线路问题，报修。

### 问题 6：网络频繁掉线或反复认证

#### 无线掉线

1. 确认是“Wi-Fi 连接本身断开”，还是“Wi-Fi 仍连接但认证掉线”。
2. 关闭随机 MAC，启用无感认证并清理旧绑定。
3. 关闭设备的 Wi-Fi 省电模式和“双 Wi-Fi/WLAN+”加速。
4. 信号弱时移动到靠近 AP 的位置；支持 5GHz 时优先使用 5GHz。
5. 断开 Wi-Fi → 关闭 Wi-Fi → 重启设备 → 重连。
6. 同一房间多人同时掉线或信号格数频繁跳变时，记录楼栋、房间和时间报修。

仅支持 Wi-Fi 4／2.4GHz 的设备，43Mbps 等较低协商速率可能来自频段本身；它会影响速度，但不直接等于认证故障。更换 Wi-Fi 6 网卡只可能改善设备兼容、频段和干扰问题，无法修复 AP、覆盖或认证服务器故障。

#### 有线随机断开

1. 重新插紧网线，换线测试墙口接触。
2. 禁用再启用以太网网卡并重新拨号。
3. 将 PPPoE 连接名称改为 `CampusNet`、`Broadband` 等纯英文名称，并在拨号时先关闭代理、VPN、Clash 和虚拟网卡软件。
4. 更新或重装有线网卡驱动；睡眠、重启或系统更新后才开始掉线时尤其要做。
5. 网卡属性 → 配置 → 电源管理，取消“允许计算机关闭此设备以节约电源”。
6. 调整系统睡眠设置；睡眠、合盖或长期空闲后 PPPoE 会话可能需要重建。
7. 删除处于异常状态的旧连接并重建，连接名称继续使用纯英文。
8. 配置断线自动重拨，或改由路由器拨号。
9. 换电脑、换线交叉测试；仍在同一墙口复现则报修。

#### 特定网卡频繁消失或断流

1. 在设备管理器确认网卡型号和驱动状态。
2. 从电脑品牌官网或网卡芯片厂商官网下载驱动，先更新并重启；仍异常时卸载后重装。
3. 更新驱动后才出问题时回退到此前稳定版本。
4. 用 USB 有线或无线网卡进行对照测试。
5. 确认是本机网卡硬件问题后，再考虑更换兼容的 Intel 等无线网卡；不要在未交叉测试前直接换硬件。

### 问题 7：网速慢、波动或疑似被限速

#### 先区分四种情况

1. **校内测速也慢**：接入链路、设备、Wi-Fi 覆盖或墙口问题。
2. **校内测速正常，所有外网都慢**：出口拥塞、后台占用或运营商产品问题。
3. **只有部分娱乐应用慢**：可能达到免费额度后的分类限速，也可能是应用/CDN 问题。
4. **速度周期性掉到零或只有几 KB/s**：认证重连、下载源、DNS/IPv6、上传占满或线路波动。

#### 正确测速

1. 使用系统浏览器打开 `test.xidian.edu.cn`。
2. 需要判断有线时，关闭 Wi-Fi，让电脑直连墙口并拨号后测试。
3. 关闭其他下载、视频、同步和系统更新。
4. 注意单位：`1 MB/s ≈ 8 Mbps`；约 12 MB/s 相当于约 100 Mbps。
5. 再用稳定的大文件下载源进行外网复测。
6. 报修时附上接入方式、校内测速结果、时间和位置。

#### 协商速率只有 100Mbps

1. 在系统网络信息中查看“链接速度／协商速率”。
2. 使用超五类以上、八芯全通的圆线；避免质量不明的扁线。
3. 检查电脑网卡、转接器和路由器端口是否支持千兆。
4. 换线、换墙口、换电脑交叉测试。
5. 原本协商到 1000Mbps，使用一段时间后降到 100Mbps，通常要重点排查网线、接头、墙线和干扰。
6. 套餐标称速率与物理链路协商是两件事；换套餐不能修复断芯或百兆端口。

#### 检查是否上传占满

上传跑满时，网页、消息发送和语音都会卡住：

1. Windows 任务管理器 → 性能 → 资源监视器 → 网络，查看“发送”最高的进程。
2. 暂停云盘、照片同步、BT/迅雷做种和 Windows 传递优化。
3. 路由器管理页查看哪台设备持续上传。
4. 卸载挂机、赚收益或 PCDN 类软件，并进行安全扫描。

持续异常上传可能触发网络侧处置；被人工禁用后需要联系人工服务恢复。

#### 每月额度与分类限速

- 免费校园网的额度、刷新时间和当前用量，以 `zfw` 页面及当期官方通知为准。
- 有线、无线和路由器使用同一账号时，流量通常归入同一账号统计。
- 达到额度后一般不是全部断网，而是部分娱乐、视频、游戏或下载资源受到限制。
- 完整限速名单会变化，不应把群内对某个应用的历史结论写成长期固定规则。
- 运营商套餐通常用于改变套餐或限速规则，不保证解决无线干扰、丢包、延迟或墙口问题。

### 问题 8：只有部分网站、应用或游戏打不开

#### 先做对照

1. 用手机热点访问同一目标：
   - 热点也打不开：优先怀疑目标站点、应用客户端或服务器。
   - 热点能打开、校园网打不开：继续排查出口、套餐、DNS、IPv6 或分类限制。
2. 检查当前是否已经完成认证。
3. 关闭代理、VPN、浏览器扩展和加速器后复测。
4. 清除应用缓存，强制刷新页面，重启客户端。
5. 核对运营商产品优先级和 PPPoE 后缀。

#### DNS：默认自动，异常时保留临时修改办法

校园网正常时，DNS 应保持自动获取；认证阶段尤其如此。确有域名解析异常时，可临时使用：

- `223.5.5.5`
- `119.29.29.29`

Windows 定位方法：

1. 运行 `ping www.baidu.com` 和 `nslookup www.baidu.com`。
2. 能访问公网 IP、域名解析失败时，问题位于 DNS。
3. 用 `ipconfig /all` 查看当前实际使用的适配器及 DNS。
4. PPPoE 场景要修改“宽带连接”的 IPv4 属性；只改 WLAN 或普通以太网可能不生效。
5. 执行 `ipconfig /flushdns` 清理缓存。
6. 排查完成后改回自动获取。

#### IPv6

1. `ipconfig` 中检查是否获得 `2001:`、`240e:` 等 IPv6 地址。
2. 确认连接属性中的 TCP/IPv6 已启用，并自动获取地址和 DNS。
3. 重新认证、重拨或重启后再测试 `https://testipv6.cn/`。
4. 手机有 IPv6、电脑没有时，清理电脑的无感认证并重新登录。
5. 电脑直拨有 IPv6、路由器没有时，检查路由器是否支持并启用 IPv6；不影响当前上网时可暂时关闭或忽略路由器 IPv6。
6. 个别网站在 IPv6 下异常时，可临时禁用 IPv6进行对照；确认后再决定是否保持关闭。

#### 视频、商店和游戏

- B 站、抖音、Steam、游戏语音等是否受限会随策略、额度和产品变化，应以同一时刻的对照测试为准。
- Steam 下载异常可重启客户端、更换下载地区，并检查上传是否占满。
- 游戏无法登录时，用手机热点测试；只在校园网复现时，再核对套餐和出口。
- 游戏内语音受影响时，可临时使用独立语音工具，或使用手机热点完成登录后再切回校园网作为应急方法。
- 微软商店图标加载失败可按 DNS 流程排查。
- Office 和正版软件下载、激活使用 `gsoft.xidian.edu.cn`；激活失败可提交信息网络报修。
- 某个镜像站或公网服务主动限制学校出口时，本地反复改设置通常无效，应换镜像源或等待对方处理。

### 问题 9：校外访问与特殊终端

#### 校外访问校内系统

校外访问一站式、图书馆、OA、教务等校内资源时：

1. 打开 `https://atrust.xidian.edu.cn` 下载 aTrust。
2. 连接地址填 `atrust.xidian.edu.cn`。
3. 账号填学号，密码使用统一身份认证密码，不是校园网上网密码。
4. 连接后再访问校内系统；回到校园网内或不再使用时关闭 aTrust。

旧版 EasyConnect 和旧 VPN 入口已经停用，不应继续按旧教程配置。安装包下载慢时，可换时间、换网络或使用可信的多线程下载工具；连接超时时先切换 Wi-Fi/移动数据、重装客户端并重试。

校园网内认证异常时要先退出 aTrust、其他 VPN、代理和虚拟网卡软件。若启用 aTrust 后本地回环地址也异常，退出相关服务后再验证。

#### 没有网口或特殊系统

- 无网口笔记本：使用 USB-C/USB 转 RJ45 转换器，或直接使用无线。
- 台式机无无线网卡：优先用有线，也可使用 USB 无线网卡；临时可用手机 USB 网络共享。
- 鸿蒙 PC 等新系统找不到 PPPoE 入口时，先使用校园无线网或由路由器拨号；具体菜单以厂商当前系统为准。
- 手机、平板、电视和游戏机不方便进行 PPPoE 时，连接已经拨号成功的宿舍路由器。

---

## 四、何时停止自查并报修

满足以下任一项即可报修信息网络故障：

- 多台设备、多根网线在同一墙口均失败。
- 同一区域多人同时掉线、无信号或满格无网。
- 651 等线路类错误完成交叉测试后仍存在。
- 校内测速持续异常，并已排除后台占用和本机问题。
- 认证入口、产品配置或账号状态需要后台核对。
- 网口松动、空口、接头脱落或墙线损坏。

### 报修入口

电脑或手机浏览器：

1. 打开 `https://repair.xidian.edu.cn`。
2. 使用统一身份认证登录。
3. 进入“信息网络报修登记”。
4. 故障类型选“网络报修”，不要误选普通设备故障。

微信：

1. 进入微信企业号“西安电子科技大学”。
2. 打开“移动门户”。
3. 选择“信息网络报修”→“我要报修”。

维修人员入宿舍时需有人在场；可由报修人或获授权的舍友配合。预约时间、页面选项异常时可刷新重试；单据被撤回时检查是否选错故障类型。

---

## 五、常用入口

| 用途 | 地址或方式 |
| --- | --- |
| 无线认证 | `https://w.xidian.edu.cn`、`https://10.255.44.33` |
| 自服务：密码、流量、设备、无感认证、产品优先级 | `https://zfw.xidian.edu.cn` |
| 信息网络报修 | `https://repair.xidian.edu.cn` |
| 校内测速 | `https://test.xidian.edu.cn` |
| 一站式办事 | `https://ehall.xidian.edu.cn` |
| 统一身份认证：修改统一身份认证密码、绑定手机、多因子验证 | `https://ids.xidian.edu.cn/authserver/login` |
| VPN / aTrust | `https://atrust.xidian.edu.cn` |
| 正版软件 | `https://gsoft.xidian.edu.cn` |
| 学习平台 | `https://learning.xidian.edu.cn` |
| 学生邮箱 | `https://mail.stu.xidian.edu.cn` |
| 校园网人工咨询 | `88201252` |
| 统一身份认证咨询 | `029-88201650` |

---

## 六、容易走弯路的地方

以下方法不是一概无效，而是不应在未定位问题前反复使用：

1. **反复改 DNS**：仅对解析异常有用；认证失败、墙口损坏或 Wi-Fi 覆盖差时无效。认证阶段应先恢复自动获取。
2. **不断切换 `ac_id`**：它是认证门户的有效补充办法，但账号密码、随机 MAC、代理或区域接入问题仍需分别处理。
3. **每次都重建 PPPoE**：适用于旧连接损坏、端口绑定异常等情况；若每次重启都必须重建，应检查驱动、快速启动、网卡状态或自动拨号配置。
4. **直接购买更高速套餐**：不能修复网线断芯、协商只有 100Mbps、Wi-Fi 干扰、丢包和墙口故障。
5. **先换网卡或路由器**：应先用另一台设备、另一根网线或电脑直连完成交叉验证。
6. **同时开 Wi-Fi 和有线排查**：容易误判实际流量走向；检查有线时先关 Wi-Fi。
7. **只看 Windows 网络图标**：图标可能滞后，应用实际联网和关闭 Wi-Fi 后能否访问才更可靠。


